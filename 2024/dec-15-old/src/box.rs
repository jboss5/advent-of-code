
struct Box {
    left_pos: usize,
    right_pos: usize
}

impl Box {
    
    pub fn new(left_pos: usize, right_pos: usize) -> Box {
        Box { left_pos, right_pos }
    }
    
    pub fn 
}